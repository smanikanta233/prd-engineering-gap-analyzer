kind = "web"
previewPath = "/"
title = "PRD Engineering Gap Analyzer"
version = "1.0.0"
id = "artifacts/prd-analyzer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22446

[services.development]
run = "pnpm --filter @workspace/prd-analyzer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/prd-analyzer", "run", "build" ]
publicDir = "artifacts/prd-analyzer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22446"
BASE_PATH = "/"
